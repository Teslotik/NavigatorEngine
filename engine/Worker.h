#ifndef WORKER_H
#define WORKER_H

/*
#include "RenderStack.h"
#include "surface.h"
#include "ds/Signal.h"

namespace engine {

using engine::RenderStack;
using engine::Surface;
using engine::ds::Signal;

/// @todo multythread
/// @todo move to the entity package?
class Worker {
public:
    Signal<> onInput;
    Signal<> onUpdate;
    Signal<RenderStack<Surface>*> onRender;
    // virtual void input() = 0;
    // virtual void update() = 0;
    // virtual void render(RenderStack<Surface> *renderer) = 0;



    // Signal<> onUpdate;
    // Signal<> onThreadUpdate;
    // Signal<> onFixedUpdate;
};

} // namespace engine
*/

#endif